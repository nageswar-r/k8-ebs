apiVersion: v1 
kind: Pod
metadata:
  name: test-pd
spec:
  containers:
  - image: nginx
    name: nginx
    volumeMounts:
    - monthPath: /var/log/nginx
      name: nginx-logs
  - image: almalinux:9
    name: almalinux
    command: ["sleep", "20000" ]
    volumeMounts:
    - monthPath: /mnt/nginx-logs
      name: nginx-logs
      readOnly: true
  volumes: 
  - name: nginx-logs
    emptyDir:
      sizelimit: 500Mi